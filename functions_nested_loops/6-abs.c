#include <stdio.h>
#include "main.h"
#include <stdlib.h>

/**
 * _abs - computes the absolute value of integer
 * @int : character to check
 * Return: 0 (Success)
 */

int _abs(int r)
{
	_abs( r );
	return (0);
}
