#include <stdio.h>
#include "main.h"
#include <stdlib.h>

/**
 * _abs - computes the absolute value of integer
 * @int r: character to check
 * Return: 0 (Success)
 *
 */

int _abs(int)
{
	int r;

	abs(r);

	return (0);
}
